# text_face_gen
Using PyTorch to create a generator-discriminator model (based on the DCGAN architecture) for face generation using single attribute (like eyes, lips, eyebrows, etc). The code uses a custom dataset of portrait images of popular people across the world (actors, CEOs, sportspersons, etc).

